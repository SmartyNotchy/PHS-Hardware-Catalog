#Pilliam can we impliment this function or something like this access the database and webserver
'''from flask import Flask, request, jsonify
teacherIds = ["apple", "banana", "cherry"]
def accessTeacher():
    id = 0
    for string in teacherIds:
        if():
            return 0'''